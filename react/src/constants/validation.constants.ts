export const REQUIRED_FIELD_MESSAGE = 'This field is required';
export const MIN_LENGTH_MESSAGE = (minLength: number) =>
  `Enter at least ${minLength} characters`;
export const MAX_LENGTH_MESSAGE = (maxLength: number) =>
  `Maximum ${maxLength} characters allowed`;
export const MIN_MESSAGE = (min: number) => `Should be at least ${min}`;
export const MAX_MESSAGE = (max: number) => `Should not be greater than ${max}`;
